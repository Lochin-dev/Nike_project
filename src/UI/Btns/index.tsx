import React from "react";
import "./index.scss";
const Button = ({ companenta }) => {
  return (
    <>
      <button className="button_com">{companenta}</button>
    </>
  );
};

export default Button;
